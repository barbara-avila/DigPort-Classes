// 1. Simple object
// Make an object 'cat' with 'name', 'color', and 'age'.
// Show the values in the console.

let cat = {
    name: 'Newy',
    color: 'Laranja',
    age: 1,
};


console.log(cat.name, cat.color, cat.age);
console.log(`O gato ${cat.name} tem ${cat.age} ano e sua cor é ${cat.color}`);